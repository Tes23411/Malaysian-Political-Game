import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.politicalworldgame',
  appName: 'political-world-game',
  webDir: 'dist'
};

export default config;
